# Tracked defaults — real secrets belong in .env.local (git-ignored).
# Relative SQLite paths resolve against the process CWD (repo root), so
# db/custom.db lives inside the repo. Prefer an absolute path when running
# the standalone build (.next/standalone) where the CWD differs.
DATABASE_URL=file:../db/custom.db
